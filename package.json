{
  "name": "ai-code-review",
  "version": "0.1.0",
  "private": true,
  "description": "Reusable GitHub Action for AI-powered pull request code review (bring-your-own-key).",
  "main": "dist/index.js",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "npm run typecheck && ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "bundle": "ncc build src/index.ts -o dist --source-map --license licenses.txt"
  },
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "minimatch": "^9.0.5"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.4.5"
  }
}
